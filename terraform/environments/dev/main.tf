# Root module — dev environment
#
# Module wiring (VPC, EKS, ECR, RDS, DNS, Secrets) lands with the epics that
# own each module (E-2 through E-7). Provider (providers.tf) and version
# constraints (versions.tf) are configured — see PETPLAT-5.

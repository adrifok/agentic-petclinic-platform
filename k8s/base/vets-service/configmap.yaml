# SPRING_DATASOURCE_URL below is dev's live RDS endpoint (as of the last
# fill) — see k8s/base/customers-service/configmap.yaml for the re-fill
# command to run once the RDS instance is recreated and this goes stale.
apiVersion: v1
kind: ConfigMap
metadata:
  name: vets-service-config
  labels:
    app.kubernetes.io/name: vets-service
    app.kubernetes.io/part-of: petclinic
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: service
data:
  SPRING_PROFILES_ACTIVE: "docker,mysql,production"
  CONFIG_SERVER_URL: "http://config-server:8888"
  SPRING_DATASOURCE_URL: "jdbc:mysql://petclinic-dev-mysql.c9eeoak8as0x.eu-central-1.rds.amazonaws.com:3306/petclinic"
